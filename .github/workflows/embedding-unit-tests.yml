name: Embedding Generation Unit Tests

on:
  push:
    branches: [main, secure-build]
    paths:
      - 'embedding-generation/**'
      - 'arm_kb_search/resources.py'
      - '.github/workflows/embedding-unit-tests.yml'
  pull_request:
    branches: [main, secure-build]
    paths:
      - 'embedding-generation/**'
      - 'arm_kb_search/resources.py'
      - '.github/workflows/embedding-unit-tests.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: embedding-generation

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest ruff

      - name: Run lint
        run: ruff check . ../arm_kb_search/resources.py

      - name: Run unit tests
        run: python -m pytest tests/ -v --tb=short
